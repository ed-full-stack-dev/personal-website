export default interface I_SiteMetadata {
    site: {
        siteMetadata: {
            title: string;
            siteUrl: string;
            description: string;
            image: string;
            twitterUsername: string;
        }
    }
}